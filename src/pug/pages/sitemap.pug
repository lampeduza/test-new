extends ../layout/layout

block variables
  -
    var links = [
      {
        text: 'Главная страница - index.html',
        anchor: 'index.html',
        title: 'Главная страница'
      }
    ];

block content
  body.sitemap
    section.map
      .map__wrapper
        ul.map__list
          each link in links
            li.map__item
              a.map__link(href= link.anchor title= link.title)= link.text